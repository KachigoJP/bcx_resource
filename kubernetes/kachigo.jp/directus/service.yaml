apiVersion: v1
kind: Service
metadata:
  name: directus-service
  namespace: default
  labels:
    app: directus
spec:
  type: ClusterIP
  selector:
    app: directus
  ports:
  - port: 80
    targetPort: 8055
    protocol: TCP
