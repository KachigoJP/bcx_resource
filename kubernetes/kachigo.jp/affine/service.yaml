apiVersion: v1
kind: Service
metadata:
  name: affine-service
  namespace: default
  labels:
    app: affine
spec:
  type: ClusterIP
  selector:
    app: affine
  ports:
  - port: 80
    targetPort: 3010
    protocol: TCP
