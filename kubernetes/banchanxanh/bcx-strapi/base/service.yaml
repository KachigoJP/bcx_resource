apiVersion: v1
kind: Service
metadata:
  name: bcx-strapi-service
  labels:
    app: bcx-strapi
spec:
  type: ClusterIP
  selector:
    app: bcx-strapi
  ports:
  - port: 80
    targetPort: 1337
    protocol: TCP
