apiVersion: v1
kind: Service
metadata:
  name: bcx-hp-host-service
  labels:
    app: bcx-hp-host
spec:
  type: ClusterIP
  selector:
    app: bcx-hp-host
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
