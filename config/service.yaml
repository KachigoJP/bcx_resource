apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: keycloak
  labels:
    app: keycloak
spec:
  type: ClusterIP  # Internal-only service
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: keycloak